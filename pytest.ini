[pytest]
testpaths = tests
markers =
    integration: testes que chamam API externa (Blip)
    etapa2: testes da etapa 2 (termo de autorizacao)
    manual_reset: testes que exigem reset manual antes de executar
